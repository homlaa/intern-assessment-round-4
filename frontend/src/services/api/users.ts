

import { User } from "../../types/user"




export const roginUser = (User & { note: string }) => {
    const { firstName, lastName, birtDate, email, password, note } = User

    // const request = 

}